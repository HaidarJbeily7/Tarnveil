{
  "name": "tarnveil",
  "private": true,
  "version": "0.0.0",
  "description": "Tarnveil — isometric browser MMO monorepo (configurable game identity via GAME config).",
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  },
  "packageManager": "pnpm@10.8.1"
}
